---
status: draft
owner: john
type: Deliverable
---

# Application questions — {{EVENT_NAME}}

> Default to the 5 core categories in `templates/application-question-bank.md`. Only
> add a situational question if this campaign specifically needs it — don't pad the
> form. Keep the hard rule: the exact venue/address is never a form field.

1. Name / email
2. Firecrawl-enabling identity (LinkedIn or website URL — required if the
   applicant-pipeline will enrich applications)
3. Segment self-ID (pull current segments from `0-context/audience-segments.md`)
4. Motivation (open text — let it carry current-state + testimonial signal too)
5. Content consent (checkbox, required — only if filming/photos are planned)

<!-- Situational, add only if needed: referral source, attendance confirmation,
     access/logistics needs (better asked post-acceptance — see the bank doc). -->
