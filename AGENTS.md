# AGENTS.md

See `CLAUDE.md` for the full operating manual and `RESOLVER.md` for skill/doc routing.
This file exists for tooling that reads `AGENTS.md` by convention (mirrors the pattern
used across the other AIOS repos).
